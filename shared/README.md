# Shared Resources

This folder contains shared configuration files used across modules.

## common-params.json

A reference parameter file showing common values used by all modules. Copy and adapt for local overrides.

> **Note:** Never commit files with real subscription IDs or tenant IDs. The `.gitignore` excludes `*.local.bicepparam` for this purpose.
